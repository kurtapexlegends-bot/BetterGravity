node_modules/
dist/
dist-electron/
*.tsbuildinfo
*.tgz
.pnpm-store/
.vite/
.playwright-cli/
.electron-builder-cache/
output/
coverage/
*.log
*.local
.DS_Store
Thumbs.db

# Local desktop/editor state
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp
*.swo

# Generated installer and release artifacts
*.dmg
*.exe
!community/**/*.exe
*.msi
*.zip
*.app/
*.dll
!community/**/*.dll
*.pdb
bin/
obj/
publish/
dist-light/
dist-native/
release/
release-*/
artifacts/
electron-builder.yml

# Private local configuration and credentials. Never commit these.
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.cer
*.pfx
*.p12

# Runtime state from a real installation, which normally lives in %APPDATA% but
# would carry an API key with it if one were ever copied in here. storage.json is
# where a plugin's settings are saved; api-calls.jsonl is the Gemini audit log.
# (No bare settings.json rule: .vscode/settings.json above is committed.)
storage.json
trust.json
*.jsonl

# Local personal backup scripts
backup.cmd
backup*.cmd

# Plugin runtime signal files
interrupt.signal

# Local scratch and extracted files
Apply-BetterGravity.bat
bootstrap.asar
main.js
make-bootstrap.cjs
